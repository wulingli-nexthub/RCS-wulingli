# Contributing

## 开发环境
- Visual Studio
- .NET Framework 4.7.2
- C# 7.3

## 代码风格与规范
- 遵循现有代码风格与命名方式，保持一致性。
- 修改代码时请保留已有注释块，不要删除或改写其含义。
- 新增/修改公共或对外可见的成员（例如 `public`、`protected`）时，优先使用 XML 文档注释（`/// <summary>` 等）。
- 对复杂逻辑、非直观的数学/坐标/角度约定、状态机分支等，补充必要的行内注释说明“为什么”而不仅是“做什么”。
- 注释语言：以中文为主（与现有代码一致），必要时可补充关键英文术语。
- 避免无意义注释（例如重复代码本身能表达的含义）。

## 提交要求
- 提交前保证解决方案可编译。
- 若修改行为，请补充/更新相应的单元测试（如项目已有测试）。